#include <stdio.h>

int main() {
int num,arm,rem,sum=0;
printf("Enter the number");
scanf("%d",&num);
arm=num;
while(num>0){
    rem=num%10;
    sum=sum+(rem*rem*rem);
    num=num/10;
}
if(arm==sum){
    printf("%d is armstrong",sum);
}
else{
    printf("%d is not armstrong",sum);
}
return 0;
}
